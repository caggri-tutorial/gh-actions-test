name: shell
on: [push]

jobs:
  run-shell-command: 
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "I am alive"
      - name: mutiline script
        run: |
          node -v
          npm -v
